@AGENTS.md

# Working agreement

The owner (giangphantruong) has pre-authorized routine build/ship actions for
this project without asking first each time, since he's often away from the
keyboard: git commits/pushes to this repo, Vercel deploys, creating test
accounts / submitting test data through the app's own forms, and installing
project dependencies. Keep moving on these rather than pausing for
confirmation.

Still always confirm before: anything destructive/hard-to-reverse (force
push, deleting the repo or database tables, dropping the Supabase project),
and anything outside this project's normal scope (new paid services,
changing account-level settings on GitHub/Vercel/Supabase beyond this repo).
